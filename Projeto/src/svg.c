#include <stdio.h>
#include <stdlib.h>
#include <string.h>


int main() {
    int a;
    for (int i = 0; i < 10000001; ++i) {
        if (a) {
            printf("Batata");
        }
        malloc(sizeof(int));
    }
}